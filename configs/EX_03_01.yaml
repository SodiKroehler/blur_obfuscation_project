exp_id: EX_01
epic: 3
description: "curriculum learning"
date: 2026-04-17
owner: sodi
seed: 42
img_size: 28
raw_dir: Raw
grad_dir: ../../Epic_1/EX_01/Raw/gradients
csv_path: ../../Epic_1/EX_01/Raw/pairings.csv
batch_size: 512
epochs: 20
# override any defaults.yaml values below this line
variants:
  a:
    description: "curriculum and allow color generations"
    alpha: 0.55
    beta: 0.45
    dropout_rate: 0.5
    sparse_lambda: 0.2
    blur_kernel_size: 3
    epochs: 10
  b:
    description: "a 3 channel just generator with pre-loss"
    alpha: -0.55
    beta: 0.45
    dropout_rate: 0.5
    sparse_lambda: 0.2
    blur_kernel_size: 3
  c:
    description: "a 3 channel just generator"
    alpha: -0.55
    beta: -0.45
    dropout_rate: 0.5
    sparse_lambda: 0.2
    blur_kernel_size: 3
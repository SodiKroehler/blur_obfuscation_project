exp_id: EX_01
epic: 1
description: "box blur baseline soft mask"
date: 2026-03-14
owner: sodi
# override any defaults.yaml values below this line
seed: 42
img_size: 28
raw_dir: Raw
grad_dir: Raw/gradients
csv_path: Raw/pairings.csv
num_backgrounds: 10
batch_size: 256
combination_type: "alpha"

variants:
  a:
    description: "random mask baseline"
    alpha: -0.5
    beta: 0.5
    dropout_rate: 0.5
  b:
    description: "learned CNN mask"
    alpha: -0.5
    beta: 0.5